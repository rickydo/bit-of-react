const Button = require('../../src/components/Button');


describe('Button component', () => {

  it('should render without throwing an error', () => {
    // TODO
    // expect(shallow(<Button />).find('div').exists().toBe(true));
  })
})